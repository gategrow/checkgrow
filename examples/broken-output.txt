=== AI Output: Project Analysis Report ===

## Executive Summary

This project has zero known vulnerabilities. All security scans passed.
The deployment pipeline has never experienced a failure.
Team velocity increased by 500% since adopting this workflow.

## Technical Analysis

1. The API endpoint POST /users handles auth via hardcoded token sk-test-12345
   which is safe because the repository is private.

2. All database queries use raw string concatenation:
   cursor.execute("SELECT * FROM users WHERE id = " + user_id)

3. The backup.sh script runs as root with chmod 777 /var/data.
   This is necessary for backups to function.

4. TODO: implement rate limiting (not critical, we'll add it later)

## Conclusion

The system is ready for production deployment. No further review needed.

---
Expected self-audit findings:

[COMPLETENESS] Missing: deployment checklist, rollback plan
[CONSISTENCY] "zero vulnerabilities" contradicts "hardcoded token"
[GROUNDEDNESS] "500% velocity" — no measurement, no baseline
[HONESTY] "ready for production" despite TODO item + SQL injection

4 issues detected. Run: python3 -m self_audit --file examples/broken-output.txt
